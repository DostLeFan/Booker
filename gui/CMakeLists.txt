file(GLOB_RECURSE BOOKER_GUI_SOURCES CONFIGURE_DEPENDS "src/*.cpp")
file(GLOB_RECURSE BOOKER_GUI_HEADERS CONFIGURE_DEPENDS "include/*.hpp")

add_executable(${GUI_NAME} ${BOOKER_GUI_SOURCES} ${BOOKER_GUI_HEADERS})

target_link_libraries(${GUI_NAME} PRIVATE ${CORE_NAME})